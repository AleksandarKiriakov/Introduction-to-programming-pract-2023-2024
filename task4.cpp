#include <iostream>
#include <math.h>

using namespace std;

int abs(int num) {
    return abs(num);
}

double fabs(double num) {
    return abs(num);
}

int main() {
    cout << abs(-10) << endl << fabs(3.154);

    return 0;
}